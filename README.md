# Gran Publicador

**Gran Publicador** — современная платформа для управления контентом и публикациями в социальных сетях, реализованная в формате Telegram Mini App. Система позволяет создавать проекты, управлять каналами в различных социальных сетях, планировать и автоматизировать публикации.

## 📋 Содержание

- [Возможности](#-возможности)
- [Технологический стек](#-технологический-стек)
- [Быстрый старт](#-быстрый-старт)
- [Установка и настройка](#-установка-и-настройка)
- [Запуск в режиме разработки](#-запуск-в-режиме-разработки)
- [Продакшн развертывание](#-продакшн-развертывание)
- [Конфигурация](#-конфигурация)
- [Структура проекта](#-структура-проекта)
- [Принцип работы](#-принцип-работы)
- [API документация](#-api-документация)
- [Тестирование](#-тестирование)
- [Разработка](#-разработка)

## ✨ Возможности

### Управление проектами
- Создание и управление множественными проектами
- Система ролей и прав доступа (Owner, Admin, Editor, Viewer)
- Приглашение участников в проекты
- Архивирование проектов с виртуальным каскадным удалением

### Управление каналами
- Поддержка множественных социальных сетей (Telegram, Instagram, VK, YouTube, TikTok, X, Facebook, LinkedIn, собственные сайты)
- Привязка каналов к проектам
- Управление учетными данными для каждого канала
- **Активация/деактивация каналов** — временная приостановка публикаций без потери видимости
- **Архивирование каналов** — долгосрочное хранение неиспользуемых каналов

#### Неактивные vs Архивированные каналы

| Аспект | Неактивный канал | Архивированный канал |
|--------|------------------|----------------------|
| **Назначение** | Временная пауза | Долгосрочное хранение |
| **Видимость** | Виден в списках с индикатором | Скрыт, доступен в разделе архива |
| **Публикация** | Заблокирована | Заблокирована |
| **Использование** | Сезонные каналы, обновление токенов, A/B тестирование | Больше не нужен, но данные сохранены |

**Подробнее:** [dev_docs/inactive_channels_concept.md](dev_docs/inactive_channels_concept.md)

### Публикации и посты
- Создание публикаций с различными типами контента (Post, Article, News, Video, Short, Story)
- Планирование публикаций с указанием даты и времени
- Автоматическая публикация по расписанию
- Статусы публикаций (Draft, Ready, Scheduled, Published, Failed, Expired)
- Связь публикаций с несколькими каналами
- **Сбор исходных текстов** — сохранение оригинальных материалов и ссылок (source texts) для каждой публикации
- **Копирование публикаций** — дублирование контента и медиафайлов в другой проект или личные черновики

### Управление медиафайлами
- Загрузка файлов напрямую или по ссылке
- Поддержка Telegram медиасистемы (file_id)
- Proxy-стриминг через внешний микросервис Media Storage
- Автоматическое определение типов медиа (Image, Video, Audio, Document)
- **Сбор метаданных изображений** — автоматическая обработка через Media Storage
- Группировка медиа в альбомы
- Единое хранилище (Media Gallery) для повторного использования файлов

### AI Генерация контента (LLM)
- Интеграция с **LLM Router** для генерации текстов
- **Backend-интеграция**: Сборка промптов и управление контекстом на стороне сервера
- **Умная обработка контекста**: Автоматическое усечение контента и исходных текстов с учетом лимитов токенов
- **Шаблоны промптов**: Личные и проектные шаблоны для быстрого доступа к часто используемым запросам
- **Счетчик токенов**: Примерная оценка размера запроса на фронтенде
- **Голосовой ввод (STT)**: Запись аудио и автоматическая транскрипция в текст запроса
- **Система безопасности**: Проверка владения и прав доступа для всех AI-операций и шаблонов
- Мягкое удаление с возможностью восстановления
- Виртуальное каскадное архивирование связанных сущностей
- Перемещение сущностей между проектами
- Окончательное удаление архивированных элементов

### Безопасность и аутентификация
- Авторизация через Telegram (проверка initData)
- JWT токены для фронтенд приложения
- API токены для внешних интеграций
- Scope-based доступ для API токенов (ограничение по проектам)
- Защита всех эндпоинтов

## 🛠 Технологический стек

### Backend
- **NestJS 11** — модульный фреймворк для Node.js
- **Fastify** — высокопроизводительный веб-сервер
- **Prisma 7** — современный ORM с типобезопасностью
- **PostgreSQL 18** — мощная реляционная база данных
- **JWT** — аутентификация и авторизация
- **Passport** — стратегии аутентификации
- **class-validator** — валидация DTO
- **Pino** — структурированное логирование

### Frontend
- **Nuxt 4** — Vue.js фреймворк с SSR поддержкой (в режиме SPA)
- **Vue 3** — прогрессивный JavaScript фреймворк
- **Nuxt UI 4** — компонентная библиотека на базе Tailwind CSS
- **Pinia** — управление состоянием
- **TipTap** — rich-text редактор
- **@tma.js/sdk-vue** — интеграция с Telegram Mini Apps
- **@nuxtjs/i18n** — интернационализация
- **Tailwind CSS 4** — utility-first CSS фреймворк

### DevOps
- **Docker** — контейнеризация
- **pnpm** — быстрый пакетный менеджер
- **Jest** — тестирование
- **ESLint** — линтинг кода
- **Prettier** — форматирование кода

## 🚀 Быстрый старт

### Требования
- **Node.js** >= 22.0.0
- **pnpm** >= 10.13.1
- **Telegram Bot Token** (получить у [@BotFather](https://t.me/BotFather))

### Установка

```bash
# Клонирование репозитория
git clone <repository-url>
cd gran-publicador

# Установка зависимостей бэкенда
pnpm install

# Установка зависимостей фронтенда
cd ui && pnpm install && cd ..
```

## ⚙️ Установка и настройка

### 1. Настройка переменных окружения

Для разработки:
```bash
cp .env.development.example .env
```

Для продакшн:
```bash
cp .env.production.example .env
```

Отредактируйте `.env` файл:

```env
# Основные настройки приложения
NODE_ENV=development
SERVER_HOST=localhost
SERVER_PORT=8080
LOG_LEVEL=debug
TZ=UTC

# PostgreSQL connection string
DATABASE_URL="postgresql://gran:gran_password@localhost:5432/gran_db?schema=public"

# ID главного администратора в Telegram (опционально)
TELEGRAM_ADMIN_ID="123456789"

# ОБЯЗАТЕЛЬНО: Секретный ключ для JWT (сгенерируйте длинную случайную строку)
JWT_SECRET="your-very-long-and-secure-secret-key-here"

# ОБЯЗАТЕЛЬНО: Токен бота от @BotFather
TELEGRAM_BOT_TOKEN="1234567890:ABCdefGHIjklMNOpqrsTUVwxyz"

# Настройки планировщика (опционально)
SCHEDULER_INTERVAL_SECONDS=60
SCHEDULER_WINDOW_MINUTES=10
POST_PROCESSING_TIMEOUT_SECONDS=30
```

### 2. Инициализация базы данных

```bash
# Создание миграций и базы данных
npx prisma migrate dev --name init

# Или только применение миграций (для продакшн)
npx prisma migrate deploy

# Генерация Prisma клиента
npx prisma generate

# Наполнение базы тестовыми данными (опционально)
npx prisma db seed
``` 

### 3. Настройка Telegram Mini App

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота и добавьте его в `.env`
3. Настройте Mini App через [@BotFather](https://t.me/BotFather):
   - Команда: `/newapp`
   - Укажите URL вашего приложения (для разработки: `http://localhost:3000`)
   - Загрузите иконку и описание

## 💻 Запуск в режиме разработки


### Подготовка окружения (Docker)

Для быстрой настройки базы данных (Postgres) и кэша (Redis) используйте подготовленный Docker Compose файл.

> **Примечание:** В примере `docker-compose.yml` volumes для Redis закомментированы. Если вам нужно сохранять данные Redis между перезапусками, раскомментируйте соответствующие строки.

```bash
# Копируем пример конфигурации
cp docker-compose.example.yml docker-compose.yml

# Запускаем сервисы (Postgres, Redis)
docker compose up -d
```

### Backend

```bash
# Установка зависимостей
pnpm install

# Применение миграций
npx prisma migrate dev

# Запуск с hot-reload
pnpm start:dev

# Запуск с отладкой
pnpm start:debug
```

Сервер будет доступен на `http://localhost:8080`

API эндпоинты: `http://localhost:8080/api/v1/*`

### Frontend

```bash
cd ui
pnpm dev
```

Интерфейс будет доступен на `http://localhost:3000`

### Режим разработки без Telegram

Для тестирования без Telegram можно использовать dev-режим:

```env
# В .env файле
VITE_DEV_MODE=true
VITE_DEV_TELEGRAM_ID=123456789
VITE_DEV_USERNAME=testuser
VITE_DEV_FIRST_NAME=Test
VITE_DEV_LAST_NAME=User
```

При этом проверка Telegram initData будет пропущена.

## 🚢 Продакшн развертывание

### Вариант 1: Docker (рекомендуется)

Подробное руководство: [docs/DEPLOYMENT.md](docs/DEPLOYMENT.md) (в разработке)

```bash
cd docker

# Отредактируйте docker-compose.yml
# Установите JWT_SECRET и TELEGRAM_BOT_TOKEN

# Запуск
docker-compose up -d

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

Приложение будет доступно на `http://localhost:8080`

### Вариант 2: Ручная сборка

**Backend:**
```bash
# Сборка
pnpm build

# Применение миграций
npx prisma migrate deploy

# Запуск
pnpm start:prod
```

**Frontend:**
```bash
cd ui

# Сборка
pnpm build

# Статические файлы будут в ui/.output/public
# Они автоматически раздаются бэкендом через Fastify Static
```

### Переменные окружения для продакшн

```env
NODE_ENV=production
SERVER_HOST=0.0.0.0
SERVER_PORT=8080
DATABASE_URL="postgresql://gran:gran_password@postgres:5432/gran_db?schema=public"
JWT_SECRET="your-production-secret"
TELEGRAM_BOT_TOKEN="your-bot-token"
TELEGRAM_ADMIN_ID="your-telegram-id"
LOG_LEVEL=info
```

## 🔧 Конфигурация

### Система конфигурации
Приложение полностью конфигурируется через **переменные окружения**. Это упрощает деплой и управление настройками в контейнеризированных средах.

Все необходимые параметры описаны в файлах `.env.development.example` и `.env.production.example`.

Для валидации конфигурации при старте приложения используется `class-validator` и `class-transformer`. Если какой-либо обязательный параметр отсутствует или некорректен, приложение завершит работу с ошибкой.

### Переменные окружения

| Переменная | Обязательная | Описание | Пример |
|------------|--------------|----------|--------|
| `NODE_ENV` | Нет | Режим работы | `development`, `production` |
| `SERVER_HOST` | Нет | Хост сервера | `localhost`, `0.0.0.0` |
| `SERVER_PORT` | Нет | Порт сервера | `8080` |
| `DATABASE_URL` | Да | Строка подключения к БД | `postgresql://user:pass@host:5432/db` |
| `JWT_SECRET` | Да | Секрет для JWT | Длинная случайная строка |
| `TELEGRAM_BOT_TOKEN` | Да | Токен Telegram бота | `1234567890:ABC...` |
| `TELEGRAM_ADMIN_ID` | Нет | ID главного администратора | `123456789` |
| `LOG_LEVEL` | Нет | Уровень логирования | `debug`, `info`, `warn`, `error` |
| `TZ` | Нет | Часовой пояс | `UTC`, `Europe/Moscow` |
| `SHUTDOWN_TIMEOUT_MS` | Нет | Таймаут graceful shutdown (мс) | `30000` |
| `VITE_DEV_MODE` | Нет | Режим разработки без Telegram | `true`, `false` |
| `VITE_DEV_TELEGRAM_ID` | Нет | Mock Telegram ID для dev | `123456789` |
| `SCHEDULER_INTERVAL_SECONDS` | Нет | Интервал проверки планировщика (сек) | `60` |
| `SCHEDULER_WINDOW_MINUTES` | Нет | Окно обработки запланированных постов (мин) | `10` |
| `POST_PROCESSING_TIMEOUT_SECONDS` | Нет | Таймаут обработки одного поста (сек) | `30` |
| `SOCIAL_POSTING_SERVICE_URL` | Да | URL микросервиса постинга | `http://social-posting:8080/api/v1` |
| `NEWS_SERVICE_URL` | Да | URL микросервиса новостей | `http://news-service:8080/api/v1` |
| `NEWS_NOTIFICATION_INTERVAL_MINUTES` | Нет | Интервал уведомлений о новостях (мин) | `10` |
| `STT_SERVICE_URL` | Да | URL микросервиса Speech-To-Text | `http://stt-gateway:8080/api/v1` |
| `STT_TIMEOUT_MS` | Нет | Таймаут STT запроса (мс) | `30000` |
| `STT_MAX_FILE_SIZE` | Нет | Максимальный размер аудио (байт) | `10485760` |

### Graceful Shutdown

Приложение поддерживает graceful shutdown при получении сигналов SIGTERM или SIGINT.

#### Процесс shutdown:
1. Прекращение приема новых HTTP запросов
2. Завершение обработки активных запросов
3. Закрытие соединения с базой данных
4. Очистка ресурсов social posting client
5. Завершение работы приложения

#### Таймаут:
По умолчанию установлен таймаут 30 секунд. Если graceful shutdown не завершится за это время, приложение будет принудительно остановлено.

Настройка таймаута:
```env
SHUTDOWN_TIMEOUT_MS=30000
```

#### Логирование:
Все этапы shutdown логируются с соответствующими метками (✅ успех, ❌ ошибка).

**Подробнее:** [dev_docs/SHUTDOWN.md](dev_docs/SHUTDOWN.md)

## 🏗 Структура проекта

```
gran-publicador/
├── src/                          # Backend (NestJS)
│   ├── common/                   # Общие утилиты, guards, decorators
│   ├── config/                   # Конфигурация приложения
│   ├── modules/                  # Бизнес-модули
│   │   ├── api-tokens/           # Управление API токенами
│   │   ├── archive/              # Система архивирования
│   │   ├── auth/                 # Аутентификация
│   │   ├── channels/             # Управление каналами
│   │   ├── posts/                # Управление постами
│   │   ├── projects/             # Управление проектами
│   │   ├── publications/         # Управление публикациями
│   │   └── users/                # Управление пользователями
│   ├── app.module.ts             # Главный модуль
│   └── main.ts                   # Точка входа
│
├── ui/                           # Frontend (Nuxt 4)
│   ├── app/
│   │   ├── components/           # Vue компоненты
│   │   ├── composables/          # Переиспользуемая логика
│   │   ├── pages/                # Страницы (роутинг)
│   │   ├── stores/               # Pinia stores
│   │   └── utils/                # Утилиты
│   └── nuxt.config.ts
│
├── prisma/                       # База данных
│   ├── migrations/               # Миграции
│   ├── schema.prisma             # Схема БД
│   └── seed.ts                   # Тестовые данные
│
├── test/                         # Тесты
│   ├── unit/                     # Unit тесты
│   └── e2e/                      # E2E тесты
│
├── docker/                       # Docker конфигурация
│
└── docs/                         # Документация
    ├── API.md                    # API документация
    ├── ARCHITECTURE.md           # Архитектура проекта
    ├── CONFIGURATION.md          # Конфигурация
    └── DEPLOYMENT.md             # Развертывание
```

**Подробная архитектура:** [docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)

## 🔄 Принцип работы

### 1. Аутентификация и авторизация

#### Для пользователей (Telegram Mini App):
1. Пользователь открывает Mini App в Telegram
2. Telegram SDK предоставляет `initData` с информацией о пользователе
3. Frontend отправляет `initData` на `/api/v1/auth/telegram`
4. Backend проверяет подпись Telegram и создает/обновляет пользователя
5. Возвращается JWT токен, который сохраняется в cookies
6. Все последующие запросы используют этот токен

#### Для внешних интеграций (API токены):
1. Пользователь создает API токен через UI (Настройки → API Tokens)
2. Указывает название и scope (доступные проекты)
3. Получает токен (показывается один раз!)
4. Использует токен в заголовке `x-api-key` или `Authorization: Bearer`

### 2. Управление проектами

```
Проект (Project)
  ├── Владелец (Owner)
  ├── Участники (Members) с ролями:
  │   ├── OWNER - полный доступ
  │   ├── ADMIN - управление участниками и каналами
  │   ├── EDITOR - создание и редактирование контента
  │   └── VIEWER - только просмотр
  ├── Каналы (Channels)
  │   └── Посты (Posts)
  └── Публикации (Publications)
      ├── Посты (Posts)
      └── Медиа (Media)
```

### 3. Публикация контента

#### Вариант 1: Прямая публикация (standalone)
1. Создается пост напрямую для канала.
2. Система автоматически создает родительскую публикацию-контейнер для обеспечения целостности данных.
3. Указывается контент, медиафайлы, дата публикации.
4. Устанавливается статус `SCHEDULED`.
5. Пост ожидает публикации в соответствии с расписанием.

#### Вариант 2: Через публикацию
1. Создается публикация (Publication) - шаблон контента
2. Публикация связывается с несколькими каналами
3. Для каждого канала создается отдельный пост
4. Посты публикуются по расписанию

### 4. Система архивирования

- **Мягкое удаление**: устанавливается `archived_at` и `archived_by`
- **Виртуальное каскадирование**: при архивации проекта архивируются все связанные каналы, публикации и посты
- **Восстановление**: можно восстановить архивированные элементы
- **Окончательное удаление**: физическое удаление из БД

### 6. Безопасность

#### Защита эндпоинтов:
- Все API эндпоинты защищены `JwtOrApiTokenGuard`
- Проверка прав доступа на уровне сервисов
- Валидация всех входных данных через DTO

#### API токены:
- Хранятся в зашифрованном виде
- Поддержка scope (ограничение по проектам)
- Отслеживание последнего использования
- Возможность отзыва в любой момент


### 5. Система отслеживания проблем

Приложение включает удобную систему мониторинга статуса проектов, каналов и публикаций через **Problems Widget** на дашборде.

#### Типы проблем:
*   **Critical (Критические)**:
    *   `FAILED` статус публикации (все посты не опубликованы)
    *   Отсутствие учетных данных (Credentials) у канала
    *   `FAILED` статус поста
*   **Warning (Предупреждения)**:
    *   `PARTIAL` статус публикации (часть постов с ошибками)
    *   `EXPIRED` статус публикации (время публикации прошло)
    *   Отсутствие активности в проекте > 3 дней
    *   "Stale" (устаревшие) или неактивные каналы

#### Навигация и фильтрация:
Виджет проблем предоставляет **deep linking**: клик по категории проблемы (например, "Failed posts") автоматически перенаправляет на соответствующий список (публикаций или каналов) с уже примененным фильтром.

## 📚 API документация

Подробная документация API доступна в файле [docs/API.md](docs/API.md)

### Базовый URL
- Development: `http://localhost:8080/api/v1`
- Production: `https://your-domain.com/api/v1`

### Аутентификация

Все эндпоинты (кроме `/auth/telegram`) требуют аутентификации:

**JWT токен (для фронтенда):**
```
Authorization: Bearer <JWT_TOKEN>
```

**API токен (для внешних интеграций):**
```
x-api-key: <API_TOKEN>
```
или
```
Authorization: Bearer <API_TOKEN>
```

### Основные эндпоинты

- **Auth**: `/api/v1/auth/*` - Аутентификация
- **Projects**: `/api/v1/projects/*` - Управление проектами
- **Channels**: `/api/v1/channels/*` - Управление каналами
- **Publications**: `/api/v1/publications/*` - Управление публикациями
    - **GET** `/user/drafts` - Получение личных черновиков пользователя
- **Posts**: `/api/v1/posts/*` - Управление постами
- **API Tokens**: `/api/v1/api-tokens/*` - Управление API токенами
- **Archive**: `/api/v1/archive/*` - Управление архивом
- **Users**: `/api/v1/users/*` - Управление пользователями
- **Health**: `/api/v1/health` - Health check

#### Личные черновики (Personal Drafts)
Личные черновики — это публикации, которые не привязаны к конкретному проекту. Они видны только создателю.

**GET** `/api/v1/publications/user/drafts`
Параметры: `limit`, `offset`, `search`, `status` (DRAFT, READY).

Полная документация с примерами запросов и ответов: [docs/API.md](docs/API.md)

## 🧪 Тестирование

### Unit тесты

```bash
# Запуск всех unit тестов
pnpm test:unit

# Запуск с покрытием
pnpm test:cov

# Запуск в watch режиме
pnpm test:watch

# Отладка unit тестов
pnpm test:unit:debug
```

### E2E тесты

```bash
# Запуск E2E тестов
pnpm test:e2e

# Отладка E2E тестов
pnpm test:e2e:debug
```

### Все тесты

```bash
pnpm test
```

## 👨‍💻 Разработка

### Полезные команды

```bash
# Backend
pnpm build              # Сборка
pnpm start:dev          # Разработка с hot-reload
pnpm start:debug        # Разработка с отладкой
pnpm start:prod         # Продакшн запуск
pnpm lint               # Линтинг
pnpm format             # Форматирование кода

# Prisma
npx prisma migrate dev     # Создание миграции
npx prisma migrate deploy  # Применение миграций
npx prisma db seed         # Заполнение тестовыми данными
npx prisma studio          # Prisma Studio (GUI для БД)
npx prisma generate                     # Генерация клиента

# Frontend
cd ui
pnpm dev                # Разработка
pnpm build              # Сборка
pnpm preview            # Просмотр сборки
```

### Структура модуля (Backend)

Каждый модуль NestJS следует единой структуре:

```
module-name/
├── dto/                    # Data Transfer Objects
│   ├── create-*.dto.ts
│   ├── update-*.dto.ts
│   └── index.ts
├── module-name.controller.ts   # HTTP контроллер
├── module-name.service.ts      # Бизнес-логика
├── module-name.module.ts       # Модуль NestJS
└── index.ts                    # Экспорты
```

### Добавление нового модуля

```bash
# Генерация модуля
nest g module modules/new-module
nest g controller modules/new-module
nest g service modules/new-module

# Создание DTO
mkdir src/modules/new-module/dto
touch src/modules/new-module/dto/create-new-module.dto.ts
touch src/modules/new-module/dto/update-new-module.dto.ts
```

### Работа с базой данных

1. Изменить `prisma/schema.prisma`
2. Создать миграцию: `node prisma-wrapper.mjs migrate dev --name migration-name`
3. Применить в продакшн: `node prisma-wrapper.mjs migrate deploy`
4. Сгенерировать клиент: `npx prisma generate`

### Code Style

- **Backend**: ESLint + Prettier
- **Database:** PostgreSQL (via Prisma ORM)
- **Caching:** Redis (Optional, falls back to in-memory)
- **Frontend**: ESLint + Prettier (конфигурация Nuxt)
- **Commits**: Conventional Commits (рекомендуется)

### Отладка

**Backend:**
```bash
# Запуск с отладчиком
pnpm start:debug

# В VS Code добавить конфигурацию:
{
  "type": "node",
  "request": "attach",
  "name": "Attach to NestJS",
  "port": 9229
}
```

**Frontend:**
```bash
# Nuxt DevTools включены по умолчанию
# Открыть в браузере: http://localhost:3000
```

### AI Функционал (LLM)

Система AI генерации выделена в отдельный модуль, который обеспечивает безопасность и корректную подготовку данных для нейросетей.

#### Ключевые особенности:
- **Безопасность**: Все операции по генерации и управлению шаблонами защищены `TemplateOwnershipGuard`. Вы можете управлять личными шаблонами (доступны везде) и шаблонами проекта (доступны всем участникам проекта).
- **Контекст**: Бэкенд автоматически собирает `content` публикации и выбранные `source texts`, форматирует их и ограничивает размер (token aware truncation), чтобы запрос не превышал лимиты модели.
- **Отказоустойчивость**: Реализована система повторных попыток (retries) с экспоненциальной задержкой на бэкенде.

#### Настройка:
```env
# URL сервиса LLM Router
LLM_SERVICE_URL="http://localhost:8081/api/v1"
# Таймаут запроса (сек)
LLM_TIMEOUT_SECS=60
```

## 📝 Лицензия

MIT

## 👥 Автор

Ivan K

---

**Полезные ссылки:**
- [API документация](docs/API.md)
- [Архитектура проекта](docs/ARCHITECTURE.md)
- [Конфигурация](docs/CONFIGURATION.md) (в разработке)
- [Развертывание](docs/DEPLOYMENT.md) (в разработке)
- [NestJS документация](https://docs.nestjs.com/)
- [Nuxt документация](https://nuxt.com/)
- [Prisma документация](https://www.prisma.io/docs)
- [Telegram Mini Apps](https://core.telegram.org/bots/webapps)
