# Отчет по анализу реализации сортировки и пагинации

## Общий статус
| Сущность | Тип пагинации | Статус реализации | Комментарий |
| :--- | :--- | :--- | :--- |
| **Проекты** | Клиентская (No Pagination) | ✅ Корректно | Загружаются полностью, сортируются на клиенте. |
| **Каналы** | Клиентская (No Pagination) | ✅ Корректно | Загружаются полностью, сортируются на клиенте. |
| **Публикации** | Серверная (Server-Side) | ❌ Некорректно | Реализована "фейковая" серверная пагинация (загрузка 1000 шт. и сортировка на клиенте). |

---

## 1. Проекты и Каналы (Client-Side)
Реализация соответствует требованию "без пагинации".
- **Бэкенд**: Эндпоинты (`projects.service.ts`, `channels.service.ts`) возвращают полные списки сущностей, доступных пользователю. Аргументы `limit/offset` либо отсутствуют, либо не используются фронтендом для постраничной навигации.
- **Фронтенд**: Используется `useSorting` для локальной сортировки и фильтрации всего массива данных. Это допустимо и соответствует текущей архитектуре для этих сущностей.

## 2. Публикации (Server-Side)
Реализация **не соответствует** требованию серверной пагинации и сортировки.
На данный момент работает гибридная схема: "Загрузить всё (до 1000 шт) и отфильтровать на клиенте".

### Выявленные проблемы

#### 🛑 Бэкенд (API & Service)
1.  **Отсутствие параметров сортировки**:
    *   Контроллер `PublicationsController.findAll` принимает только `limit`, `offset`, `status`, `projectId`.
    *   Параметры `sortBy` и `sortOrder` **отсутствуют** в контракте API.
2.  **Хардкод сортировки**:
    *   В `PublicationsService` жестко прописано `orderBy: { createdAt: 'desc' }`. Изменить порядок сортировки через API невозможно.
3.  **Недостаток фильтров**:
    *   Бэкенд не поддерживает фильтрацию по: `channelId`, `searchQuery` (поиск по тексту), `socialMedia`, `language`, `issues` (проблемные посты).
    *   Сейчас фронтенд запрашивает "всё" и фильтрует это сам. При переходе на честную серверную пагинацию (limit=20) эти фильтры перестанут работать корректно (будут искать только на первой странице).

#### 🛑 Фронтенд (UI)
1.  **Fake Pagination**:
    *   В `publications/index.vue` вызывается `fetchUserPublications({ limit: 1000 })`.
    *   Пагинация (`paginatedPublications`) строится через `slice()` локального массива.
2.  **Клиентская сортировка**:
    *   Используется `useSorting`, который сортирует массив в браузере.
    *   При `limit=20` (настоящая пагинация) клиентская сортировка будет сортировать только 20 элементов текущей страницы, что ломает UX.

---

## План исправлений для Публикаций

### Этап 1: Бэкенд (Расширение API)
Необходимо обновить `PublicationsController` и `PublicationsService`:

1.  **DTO**: Добавить `sortBy` (enum: `createdAt`, `scheduledAt`, `publishedAt`, `postDate`) и `sortOrder` (`asc`, `desc`).
2.  **Фильтры**: Добавить в `findAll` поддержку фильтрации по:
    *   `channelId` (связанные посты)
    *   `search` (текстовый поиск по title/content)
    *   `language`
3.  **Service**:
    *   Заменить `orderBy: { createdAt: 'desc' }` на динамический объект.
    *   Применить фильтры в `where`.

### Этап 2: Фронтенд (Рефакторинг)
1.  **Composable**:
    *   Обновить `usePublications` для передачи новых параметров в API.
2.  **Страница (`index.vue`)**:
    *   Убрать `limit: 1000`. Установить `limit: 20`.
    *   Отключить клиентскую сортировку в `useSorting` (использовать его только для хранения стейта `sortBy`/`sortOrder`).
    *   Настроить `watch`: при изменении страницы, фильтров или сортировки — вызывать `fetchUserPublications` с новыми параметрами.
    *   Убрать вычисляемое свойство `filteredPublications` (теперь фильтрацию делает сервер). Показываем то, что пришло в `publications.value`.
