# Problem Propagation Audit Report

## Дата: 2026-01-09

## Обзор

Данный отчёт анализирует функциональность "problem propagation" (распространение проблем) в проекте Gran Publicador — отображение ошибок на всех уровнях иерархии: публикация/пост, канал, проект, дашборд, сайдбар.

---

## 1. Архитектура системы проблем

### 1.1 Уровни иерархии

```
Дашборд/Сайдбар
    └── Проект
        └── Канал
            └── Публикация
                └── Пост
```

### 1.2 Ключевые файлы

| Файл | Назначение |
|------|------------|
| `ui/app/composables/usePublications.ts` | `getPublicationProblems()`, `getPublicationProblemLevel()`, `getPostProblemLevel()` |
| `ui/app/composables/useChannels.ts` | `getChannelProblems()`, `getChannelProblemLevel()` |
| `ui/app/composables/useProjects.ts` | `getProjectProblems()`, `getProjectProblemLevel()` |
| `ui/app/components/common/ProblemBanner.vue` | Баннер с проблемами на страницах сущностей |
| `ui/app/components/common/ProblemIndicator.vue` | Индикатор проблем (НЕ ИСПОЛЬЗУЕТСЯ) |
| `ui/app/components/common/SocialIcon.vue` | Иконка соцсети с точкой-индикатором проблемы |
| `ui/app/components/dashboard/ProblemsWidget.vue` | Виджет проблем на дашборде |

---

## 2. Анализ распространения проблем снизу вверх

### 2.1 Пост (Post) → Публикация (Publication)

**Статус: ✅ КОРРЕКТНО**

```typescript
// usePublications.ts - getPublicationProblems()
function getPublicationProblems(publication: PublicationWithRelations) {
    const problems = []
    
    // Проверка статуса публикации
    if (publication.status === 'FAILED')
        problems.push({ type: 'critical', key: 'allPostsFailed' })
    
    if (publication.status === 'PARTIAL')
        problems.push({ type: 'warning', key: 'somePostsFailed', count: failedCount })
    
    if (publication.status === 'EXPIRED')
        problems.push({ type: 'warning', key: 'publicationExpired' })

    // Проверка ошибок постов независимо от статуса публикации
    if (failedPostsCount > 0 && status !== 'FAILED' && status !== 'PARTIAL')
        problems.push({ type: 'warning', key: 'postsHaveErrors', count: failedPostsCount })
    
    // Проверка проблем каналов постов
    if (hasChannelProblems(publication))
        problems.push({ type: 'warning', key: 'channelProblems' })
}
```

**Проблемы постов распространяются на публикацию:** ✅ Да

---

### 2.2 Публикация → Канал

**Статус: ⚠️ ЧАСТИЧНО**

```typescript
// useChannels.ts - getChannelProblems()
function getChannelProblems(channel: any) {
    const problems = []
    
    // Критические
    if (!channel.credentials || Object.keys(channel.credentials).length === 0)
        problems.push({ type: 'critical', key: 'noCredentials' })
    
    if (channel.failedPostsCount > 0)
        problems.push({ type: 'critical', key: 'failedPosts', count: channel.failedPostsCount })
    
    // Предупреждения
    if (channel.isStale)
        problems.push({ type: 'warning', key: 'staleChannel' })
    
    if (!channel.isActive)
        problems.push({ type: 'warning', key: 'inactiveChannel' })
}
```

**Проблемы публикаций распространяются на канал через `failedPostsCount`:** ✅ Да
**Ошибки каналов НЕ показываются у публикаций (только наоборот):** ✅ Корректно

---

### 2.3 Канал → Проект

**Статус: ⚠️ ЧАСТИЧНО**

```typescript
// useProjects.ts - getProjectProblems()
function getProjectProblems(project: Project | any) {
    const problems = []
    
    // Критические - failed posts
    if (project.failedPostsCount > 0)
        problems.push({ type: 'critical', key: 'failedPosts', count: project.failedPostsCount })
    
    // Предупреждения
    if (project.problemPublicationsCount > 0)
        problems.push({ type: 'warning', key: 'problemPublications', count })
    
    if (project.staleChannelsCount > 0)
        problems.push({ type: 'warning', key: 'staleChannels', count })
    
    // Нет активности более 3 дней
    if (diffDays > 3)
        problems.push({ type: 'warning', key: 'noRecentActivity' })
}
```

**Найденные проблемы:**

1. ⚠️ **Проект не агрегирует все проблемы каналов** — только `staleChannelsCount` и `failedPostsCount`
2. ❌ **Каналы без credentials (`noCredentials`) НЕ учитываются** на уровне проекта
3. ❌ **Неактивные каналы (`inactiveChannel`) НЕ учитываются** на уровне проекта

---

### 2.4 Проект → Дашборд/Сайдбар

**Статус: ✅ КОРРЕКТНО**

`DashboardProjectListItem.vue` использует `getProjectProblems()` и `getProjectProblemLevel()` для отображения индикатора и бейджей.

---

## 3. Проверка обработки всех проблемных статусов

### 3.1 PublicationStatus enum

| Статус | Обрабатывается | Тип проблемы | Где отображается |
|--------|---------------|--------------|------------------|
| `DRAFT` | ✅ | — | Нет проблемы |
| `READY` | ✅ | — | Нет проблемы |
| `SCHEDULED` | ✅ | — | Нет проблемы |
| `PROCESSING` | ✅ | — | Нет проблемы |
| `PUBLISHED` | ✅ | — | Нет проблемы |
| `PARTIAL` | ✅ | warning | Баннер, список, виджет |
| `FAILED` | ✅ | critical | Баннер, список, виджет |
| `EXPIRED` | ✅ | warning | Баннер, список, виджет |

### 3.2 PostStatus enum

| Статус | Обрабатывается | Тип проблемы |
|--------|---------------|--------------|
| `PENDING` | ✅ | — |
| `PUBLISHED` | ✅ | — |
| `FAILED` | ✅ | critical |

**Все статусы обрабатываются:** ✅

---

## 4. Отображение errorMessage поста

**Статус: ✅ КОРРЕКТНО**

В `PostEditBlock.vue` (строки 417-426):

```vue
<UAlert
   v-if="props.post?.errorMessage"
   color="error"
   variant="soft"
   icon="i-heroicons-exclamation-circle"
   :title="t('common.error')"
   :description="props.post.errorMessage"
   class="mb-4"
/>
```

**errorMessage показывается:** ✅ Да, при раскрытии блока поста

---

## 5. Проверка UI компонентов

### 5.1 Индикаторы (цветные точки)

| Компонент | Использует | Статус |
|-----------|-----------|--------|
| `SocialIcon.vue` | `problemLevel` prop | ✅ Работает |
| `DashboardProjectListItem.vue` | `getIndicatorColor()` | ✅ Работает |
| `ChannelListItem.vue` | `channelProblemLevel` | ✅ Работает |
| `ChannelCard.vue` | `channelProblemLevel` | ✅ Работает |

**Цветовая схема:**
- 🟢 Зелёный (`bg-emerald-500`) — без проблем
- 🟡 Жёлтый/Оранжевый (`bg-orange-500`) — warning
- 🔴 Красный (`bg-red-500`) — critical

### 5.2 Баннеры на страницах сущностей

| Страница | Баннер | Статус |
|----------|--------|--------|
| `/publications/[id].vue` | `CommonProblemBanner` | ✅ Работает |
| `/channels/[id]/index.vue` | `CommonProblemBanner` | ✅ Работает |
| `/projects/[id]/index.vue` | `CommonProblemBanner` | ✅ Работает |

### 5.3 Иконки проблем

| Тип | Иконка | Использование |
|-----|--------|---------------|
| Critical | `i-heroicons-x-circle` | Баннеры, списки |
| Warning | `i-heroicons-exclamation-triangle` | Баннеры, списки |
| Expired | `i-heroicons-clock` | Публикации |
| No credentials | `i-heroicons-exclamation-triangle` | Каналы |
| Stale | `i-heroicons-clock` | Каналы |

### 5.4 Элементы списков и карточки

| Компонент | Проблемы отображаются | Как |
|-----------|----------------------|-----|
| `PublicationListItem.vue` | ✅ | Иконки с тултипами |
| `PublicationCard.vue` | ✅ | Иконки с тултипами |
| `ChannelListItem.vue` | ✅ | Бейджи, иконки, индикатор в SocialIcon |
| `ChannelCard.vue` | ✅ | Бейджи, иконки, индикатор в SocialIcon |
| `DashboardProjectListItem.vue` | ✅ | Цветной индикатор, бейджи ошибок/предупреждений |

### 5.5 Блоки информации на страницах сущностей

**Текущее поведение:**
- Метаданные сущности (статус, язык, тип и т.д.) **НЕ содержат** информации о проблемах
- Проблемы вынесены в отдельный баннер `ProblemBanner`

**Статус: ✅ КОРРЕКТНО** — следует принципу разделения ответственности

---

## 6. Структура кода и дублирование

### 6.1 Положительные аспекты

1. ✅ **Централизованные функции** — вся логика определения проблем в composables
2. ✅ **Единый формат проблем** — `{ type: 'critical' | 'warning', key: string, count?: number }`
3. ✅ **Переиспользуемые компоненты** — `ProblemBanner`, `SocialIcon`
4. ✅ **Локализация** — все тексты проблем в i18n файлах
5. ✅ **Двухуровневая система** — critical и warning

### 6.2 Проблемы

1. ⚠️ **`ProblemIndicator.vue` НЕ ИСПОЛЬЗУЕТСЯ** — компонент создан, но нигде не импортируется
2. ⚠️ **Дублирование логики иконок** — в `PublicationListItem`, `PublicationCard`, `ProblemBanner` дублируется логика выбора иконки
3. ⚠️ **Дублирование вычисления проблем** — на странице проекта (`/projects/[id]/index.vue`) проблемы вычисляются локально вместо использования `getProjectProblems()`
4. ⚠️ **Смешивание ответственности** — `ProblemsWidget.vue` самостоятельно агрегирует данные вместо использования общих функций

---

## 7. Готовность к продакшену

### 7.1 Критические проблемы ❌

1. **Неполная агрегация проблем каналов на уровне проекта**
   - `noCredentials` каналов не отображается
   - `inactiveChannel` не агрегируется
   - Это может привести к тому, что проект выглядит "здоровым", когда у него есть каналы без credentials

2. **Дублирование логики на странице проекта**
   - `/projects/[id]/index.vue` вычисляет `projectProblems` локально (строки 178-241), но не использует `getProjectProblems()` из composable

### 7.2 Важные проблемы ⚠️

1. **Неиспользуемый компонент** — `ProblemIndicator.vue` создан, но не используется. Следует либо удалить, либо внедрить.

2. **Несогласованность подсчёта в `ProblemsWidget.vue`**
   - Виджет подсчитывает проблемы самостоятельно, дублируя логику composables
   - Возможна рассинхронизация при изменении логики

3. **Отсутствует отображение `errorMessage` в списках**
   - `errorMessage` поста показывается только при раскрытии `PostEditBlock`
   - В списках публикаций/каналов конкретное сообщение об ошибке не видно

### 7.3 Незначительные проблемы

1. Дублирование логики выбора иконок проблем в нескольких компонентах
2. Нет unit-тестов для функций определения проблем

---

## 8. Соответствие лучшим практикам

### 8.1 Соответствует ✅

| Практика | Статус |
|----------|--------|
| Single Source of Truth для логики | Частично (composables) |
| Типизация | ✅ TypeScript |
| Локализация | ✅ i18n |
| Переиспользование компонентов | ✅ |
| Separation of Concerns | ✅ |
| Accessibility (tooltips) | ✅ |

### 8.2 Не соответствует ❌

| Практика | Проблема |
|----------|----------|
| DRY | Дублирование логики в нескольких местах |
| Dead code elimination | `ProblemIndicator.vue` не используется |
| Consistency | Разные компоненты используют разные подходы к отображению проблем |

---

## 9. Рекомендации

### 9.1 Высокий приоритет

1. **Добавить агрегацию всех проблем каналов в `getProjectProblems()`:**
   ```typescript
   // Добавить в useProjects.ts
   if (project.noCredentialsChannelsCount > 0) {
       problems.push({ type: 'critical', key: 'channelsWithoutCredentials', count })
   }
   if (project.inactiveChannelsCount > 0) {
       problems.push({ type: 'warning', key: 'inactiveChannels', count })
   }
   ```

2. **Рефакторинг страницы проекта** — использовать `getProjectProblems()` вместо локального вычисления

3. **Добавить backend поддержку** — агрегировать `noCredentialsChannelsCount` и `inactiveChannelsCount` в API проекта

### 9.2 Средний приоритет

1. **Удалить или использовать `ProblemIndicator.vue`**
   - Рекомендация: использовать в `PublicationListItem`, `PublicationCard` для унификации

2. **Централизовать логику выбора иконок:**
   ```typescript
   // utils/problems.ts
   export function getProblemIcon(problemKey: string): string {
       const icons: Record<string, string> = {
           allPostsFailed: 'i-heroicons-x-circle',
           somePostsFailed: 'i-heroicons-exclamation-triangle',
           publicationExpired: 'i-heroicons-clock',
           // ...
       }
       return icons[problemKey] || 'i-heroicons-exclamation-triangle'
   }
   ```

3. **Рефакторинг `ProblemsWidget.vue`** — использовать общие функции из composables

### 9.3 Низкий приоритет

1. Добавить unit-тесты для функций определения проблем
2. Показывать превью `errorMessage` в списках (truncated)
3. Добавить анимации при появлении индикаторов проблем

---

## 10. Заключение

### Общая оценка: 7/10

**Положительные стороны:**
- Хорошая архитектура с централизованными composables
- Правильное направление распространения (снизу вверх)
- Качественная локализация
- Удобные UI компоненты

**Области для улучшения:**
- Неполная агрегация проблем каналов на уровне проекта (критично)
- Дублирование кода в нескольких местах
- Неиспользуемый компонент `ProblemIndicator.vue`

**Готовность к продакшену:** ⚠️ Требуются доработки перед релизом

Рекомендуется:
1. Исправить агрегацию проблем каналов на уровне проекта
2. Рефакторинг страницы проекта для использования общей функции
3. Решить вопрос с `ProblemIndicator.vue`
