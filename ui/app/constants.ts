// Maximum number of projects to fetch for selects
export const PROJECTS_FETCH_LIMIT = 100;

// Maximum number of channels to fetch from the backend for selects/previews
export const CHANNELS_FETCH_LIMIT = 1000;

// Default limit for a single page in selects
export const SELECT_ITEMS_LIMIT = 50;

// Default page size for pagination
export const DEFAULT_PAGE_SIZE = 15;

// Number of automatic infinite scroll loads before showing "Load More" button
export const INFINITE_SCROLL_AUTO_LIMIT = 3;
