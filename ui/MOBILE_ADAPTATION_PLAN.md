# План адаптации UI под мобильные устройства

## Выявленные глобальные проблемы

### 🔴 Критические проблемы

#### 1. Viewport ограничения
**Проблема:** `user-scalable=no` и `maximum-scale=1` запрещают зум
**Файл:** `ui/nuxt.config.ts` (строка 31)
**Решение:** Изменить на `width=device-width, initial-scale=1`

#### 2. Отсутствие адаптивных CSS правил
**Проблема:** Нет медиа-запросов для мобильных устройств
**Файл:** `ui/app/assets/css/main.css`
**Решение:** Добавить:
- Адаптивные размеры шрифтов
- Touch-friendly минимальные размеры элементов (44x44px)
- Адаптивные отступы и padding

#### 3. Формы не адаптированы
**Проблема:** Двухколоночные сетки на мобильных
**Файлы:** 
- `ui/app/components/forms/PublicationForm.vue`
- `ui/app/components/forms/ProjectForm.vue`
**Решение:** Использовать `grid-cols-1 md:grid-cols-2` вместо фиксированных двух колонок

#### 4. Модальные окна
**Проблема:** Могут не помещаться на маленьких экранах
**Решение:** Добавить `max-h-screen overflow-y-auto` для модалов

### 🟡 Средние проблемы

#### 5. Header переполнен на мобильных
**Файл:** `ui/app/components/layout/Header.vue`
**Решение:** 
- Скрывать некритичные элементы на xs экранах
- Уменьшить размеры иконок/кнопок

#### 6. Bottom Navigation safe-area
**Файл:** `ui/app/components/layout/BottomNavigation.vue`
**Решение:** Проверить корректность `env(safe-area-inset-bottom)`

#### 7. Touch targets
**Проблема:** Нет минимальных размеров для кликабельных элементов
**Решение:** Добавить глобальное правило `.touch-target { min-width: 44px; min-height: 44px; }`

### 🟢 Хорошо реализовано

- ✅ Адаптивный sidebar с overlay
- ✅ Bottom Navigation для мобильных
- ✅ FAB кнопка
- ✅ Tailwind responsive классы
- ✅ Safe area support

## Приоритетный план исправлений

### Этап 1: Критические исправления (1-2 часа)
1. Исправить viewport в `nuxt.config.ts`
2. Добавить базовые медиа-запросы в `main.css`
3. Исправить формы на одноколоночные для мобильных

### Этап 2: Улучшения UX (2-3 часа)
4. Оптимизировать Header для маленьких экранов
5. Добавить touch-friendly размеры
6. Исправить модальные окна

### Этап 3: Полировка (1-2 часа)
7. Тестирование на реальных устройствах
8. Исправление edge cases
9. Оптимизация производительности

## Конкретные изменения кода

### 1. nuxt.config.ts
```typescript
// Было:
viewport: 'width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no'

// Должно быть:
viewport: 'width=device-width, initial-scale=1'
```

### 2. main.css
```css
/* Добавить в конец файла */

/* Mobile-first responsive utilities */
@layer utilities {
  /* Touch-friendly targets */
  .touch-target {
    @apply min-w-[44px] min-h-[44px];
  }
  
  /* Mobile spacing */
  .mobile-container {
    @apply px-4 sm:px-6 lg:px-8;
  }
  
  /* Responsive text */
  .text-responsive-lg {
    @apply text-lg sm:text-xl lg:text-2xl;
  }
  
  .text-responsive-base {
    @apply text-sm sm:text-base;
  }
}

/* Mobile-specific overrides */
@media (max-width: 640px) {
  /* Ensure modals fit on screen */
  [role="dialog"] {
    max-height: 90vh;
    overflow-y: auto;
  }
  
  /* Reduce padding in cards */
  .app-card {
    @apply p-3;
  }
  
  /* Stack form grids */
  .form-grid-2col {
    @apply grid-cols-1;
  }
}
```

### 3. PublicationForm.vue
```vue
<!-- Было: -->
<div :class="GRID_LAYOUTS.twoColumn">

<!-- Должно быть: -->
<div class="grid grid-cols-1 md:grid-cols-2 gap-4">
```

### 4. Header.vue
```vue
<!-- Добавить адаптивность для кнопок -->
<UButton
  variant="ghost"
  color="neutral"
  class="p-1 sm:p-2 touch-target"
>
```

## Тестирование

### Устройства для тестирования:
- [ ] iPhone SE (320px)
- [ ] iPhone 12/13 (390px)
- [ ] iPhone 14 Pro Max (430px)
- [ ] Android (360px - 412px)
- [ ] iPad Mini (768px)
- [ ] iPad Pro (1024px)

### Сценарии тестирования:
- [ ] Создание публикации
- [ ] Редактирование публикации
- [ ] Навигация по приложению
- [ ] Работа с модальными окнами
- [ ] Работа с формами
- [ ] Скролл длинных списков

## Метрики успеха

- Все интерактивные элементы минимум 44x44px
- Формы помещаются на экран без горизонтального скролла
- Модальные окна не обрезаются
- Текст читаем без зума
- Lighthouse Mobile Score > 90
