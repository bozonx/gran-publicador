### План реализации:
1.  **Исправление звездочки обязательности**:
    *   В файле `ui/app/pages/projects/[id]/content-library.vue` удалю ручную вставку звездочки `<span class="text-red-500 text-base">*</span>` из шаблона `#label`.
    *   Оставлю проп `required` у `UFormField`, чтобы компонент сам управлял отображением индикатора обязательности в соответствии с дизайн-системой.
2.  **Поля на всю ширину**:
    *   Добавлю класс `w-full` ко всем элементам ввода (`UInput`, `UTextarea`) внутри форм создания и редактирования.
    *   Убежусь, что `UFormField` также растягивается на всю ширину.
3.  **Улучшение визуальной иерархии**:
    *   Проверю отступы и группировку полей, чтобы они соответствовали современным стандартам UI, как указано в инструкциях.
