# 📋 Отчет: Оставшиеся задачи по функциональности "Подпись автора"

## ✅ Выполнено

### 1. **Удалено архивирование подписей**
- ✅ Удалены поля `archivedAt` и `archivedBy` из схемы Prisma
- ✅ Создана и применена миграция `20260117000811_remove_signature_archiving`
- ✅ Заменены методы `archive()` и `restore()` на `delete()` с проверкой использования
- ✅ Обновлены API endpoints (удален `/restore`, изменен `/delete`)
- ✅ Удалены фильтры `archivedAt: null` из всех запросов
- ✅ Обновлены TypeScript типы
- ✅ Обновлены переводы (ru-RU, en-US)

### 2. **Добавлена визуальная индикация дефолтной подписи**
- ✅ Добавлена иконка звезды (⭐) для дефолтных подписей в селекторе
- ✅ Улучшена структура dropdown items

### 3. **Автоматический выбор дефолтной подписи**
- ✅ При создании нового поста автоматически выбирается дефолтная подпись пользователя для выбранного канала
- ✅ Работает через watcher на `formData.channelId`

---

## 🔴 Критические задачи (требуют решения)

### 1. **Валидация authorSignatureId при сохранении поста**
**Проблема**: Нет проверки существования и доступности подписи при создании/обновлении поста.

**Где**: `src/modules/posts/posts.service.ts` или создать отдельный validator

**Что сделать**:
```typescript
// Добавить метод валидации
async validateAuthorSignature(signatureId: string, channelId: string, userId: string) {
  // Проверить, что это либо пресет, либо существующая пользовательская подпись
  const preset = PRESET_SIGNATURES.find(p => p.id === signatureId);
  if (preset) return true;
  
  const signature = await this.prisma.authorSignature.findUnique({
    where: { id: signatureId }
  });
  
  if (!signature) {
    throw new NotFoundException('Author signature not found');
  }
  
  if (signature.channelId !== channelId) {
    throw new BadRequestException('Signature does not belong to this channel');
  }
  
  // Проверить доступ через AuthorSignaturesService.checkAccess()
  const hasAccess = await this.authorSignaturesService.checkAccess(signatureId, userId);
  if (!hasAccess) {
    throw new ForbiddenException('No access to this signature');
  }
  
  return true;
}
```

**Приоритет**: 🔴 Критический

---

### 2. **Обработка удаленных подписей в опубликованных постах**
**Проблема**: Если подпись удалена, но используется в опубликованных постах (PUBLISHED), при повторной публикации подпись не будет найдена.

**Варианты решения**:
1. **Мягкий**: При публикации просто игнорировать несуществующую подпись (текущее поведение)
2. **Средний**: Показывать предупреждение в UI, что подпись не найдена
3. **Жесткий**: Хранить копию контента подписи в мета-данных поста при первой публикации

**Рекомендация**: Вариант 2 - показывать предупреждение

**Приоритет**: 🟡 Средний

---

## 🟡 Важные задачи (желательно сделать)

### 3. **Оптимизация N+1 запросов в превью публикации**
**Проблема**: В `pages/publications/[id]/index.vue` для каждого поста делается отдельный запрос `fetchByChannel`.

**Решение**:
```typescript
// Загрузить все подписи для всех каналов публикации одним запросом
const channelIds = currentPublication.value.posts.map(p => p.channelId)
const allSignatures = await Promise.all(
  [...new Set(channelIds)].map(id => fetchByChannel(id))
)
// Создать Map для быстрого доступа
const signaturesMap = new Map()
allSignatures.flat().forEach(sig => signaturesMap.set(sig.id, sig))
```

**Приоритет**: 🟡 Средний

---

### 4. **Unit-тесты для AuthorSignaturesService**
**Что тестировать**:
- ✅ Создание подписи
- ✅ Автоматическое снятие isDefault при установке новой
- ✅ Проверка доступа (creator, project owner, project admin)
- ✅ Удаление с проверкой использования
- ✅ Валидация уникальности (userId, channelId, name)

**Файл**: `test/unit/author-signatures.service.spec.ts`

**Приоритет**: 🟡 Средний

---

### 5. **Предупреждение при удалении используемой подписи**
**Проблема**: Сейчас выбрасывается ошибка, но не показывается количество постов.

**Решение**: В UI показывать более информативное сообщение:
```vue
// В AuthorSignatureManager.vue
catch (error: any) {
  const toast = useToast()
  // Парсить сообщение об ошибке и извлекать количество постов
  const match = error.message.match(/(\d+) pending or failed posts/)
  const count = match ? match[1] : '?'
  
  toast.add({
    title: t('common.error'),
    description: t('authorSignature.delete_in_use_error', { count }),
    color: 'error'
  })
}
```

**Приоритет**: 🟡 Средний

---

## 🟢 Желательные улучшения (можно отложить)

### 6. **Drag-and-drop для изменения порядка подписей**
**Где**: `AuthorSignatureManager.vue`

**Библиотека**: `@vueuse/integrations` + `sortablejs`

**Приоритет**: 🟢 Низкий

---

### 7. **Live preview финального текста с подписью**
**Где**: `PostEditBlock.vue`

**Что добавить**: Computed property, который форматирует финальный текст с учетом шаблона и подписи.

**Приоритет**: 🟢 Низкий

---

### 8. **Rate limiting на создание подписей**
**Где**: Backend - добавить декоратор `@Throttle()` на endpoint создания

**Лимит**: Например, 10 подписей в минуту

**Приоритет**: 🟢 Низкий

---

### 9. **Поиск по подписям в селекторе**
**Где**: `AuthorSignatureSelector.vue`

**Реализация**: Добавить фильтрацию options по введенному тексту

**Приоритет**: 🟢 Низкий

---

### 10. **Копирование подписи между каналами**
**Где**: `AuthorSignatureManager.vue`

**UI**: Кнопка "Copy to another channel" в карточке подписи

**Приоритет**: 🟢 Низкий

---

### 11. **Показывать количество постов, использующих подпись**
**Где**: `AuthorSignatureManager.vue`

**Реализация**: Добавить счетчик в карточку подписи

**API**: Добавить endpoint `/author-signatures/:id/usage-count`

**Приоритет**: 🟢 Низкий

---

## 🐛 Известные баги

### TypeScript lint ошибки в селекторах
**Файлы**: 
- `PostEditBlock.vue:747` - Property 'label' does not exist on type 'SelectMenuSlots'
- `AuthorSignatureSelector.vue:113` - Property 'label' does not exist on type 'SelectMenuSlots'

**Причина**: Проблема с типизацией компонента USelectMenu из UI библиотеки

**Решение**: Добавить `@ts-ignore` или обновить типы UI библиотеки

**Приоритет**: 🟢 Низкий (не влияет на функциональность)

---

## 📊 Итоговая статистика

| Категория | Количество | Выполнено |
|-----------|-----------|-----------|
| Критические | 2 | 0 |
| Важные | 3 | 0 |
| Желательные | 6 | 0 |
| Баги | 1 | 0 |
| **Всего** | **12** | **0** |

**Выполнено в этой сессии**: 3 задачи (удаление архивирования + 2 улучшения)

---

## 🎯 Рекомендуемый порядок выполнения

1. 🔴 **Задача #1** - Валидация authorSignatureId (критично для безопасности)
2. 🟡 **Задача #5** - Улучшить сообщение об ошибке при удалении (быстро, улучшает UX)
3. 🟡 **Задача #4** - Написать unit-тесты (важно для стабильности)
4. 🟡 **Задача #3** - Оптимизация N+1 запросов (если есть проблемы с производительностью)
5. 🟡 **Задача #2** - Обработка удаленных подписей (если пользователи жалуются)
6. 🟢 Остальные задачи - по необходимости

---

## 💡 Дополнительные замечания

1. **Миграция данных**: Если в БД были архивированные подписи, они теперь стали обычными (archivedAt удален). Это нормально.

2. **Обратная совместимость**: Старые посты с несуществующими authorSignatureId будут работать - просто подпись не будет добавлена при публикации.

3. **Производительность**: Текущая реализация достаточно эффективна для большинства случаев. Оптимизация нужна только при >1000 подписей на канал.

4. **Безопасность**: Основная проблема - отсутствие валидации authorSignatureId. Это нужно исправить в первую очередь.
