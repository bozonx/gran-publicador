# Аудит функциональности личных черновиков и заготовок (шаблонов промптов)

**Дата:** 2026-01-15  
**Цель:** Проверка полноты реализации, логичности архитектуры и соответствия лучшим практикам

---

## 📋 Краткое резюме

### ✅ Что реализовано хорошо

1. **Четкое разделение сущностей**: Личные черновики (Publications без projectId) и шаблоны промптов (LlmPromptTemplate) - это разные сущности с разным назначением
2. **Безопасность**: Реализованы проверки прав доступа на уровне контроллеров и сервисов
3. **Валидация**: Корректная валидация бизнес-правил для личных черновиков
4. **UI/UX**: Удобный интерфейс для работы с обеими функциональностями
5. **Тестирование**: Написаны unit-тесты для критических компонентов

### ⚠️ Найденные проблемы

1. **Терминология**: Смешение понятий "черновики" и "заготовки" может вводить в заблуждение
2. **Документация**: Недостаточно информации о личных черновиках в README
3. **Функциональность**: Отсутствует возможность конвертации личного черновика в публикацию проекта
4. **UI**: Нет явного индикатора личных черновиков в списках публикаций

---

## 1. Личные черновики (Personal Drafts)

### 1.1 Архитектура и модель данных

#### ✅ Правильная реализация

**Схема БД (prisma/schema.prisma:137-194)**
```prisma
model Publication {
  id                 String  @id @default(uuid())
  projectId          String? @map("project_id")  // NULL для личных черновиков
  createdBy          String? @map("created_by")
  // ... остальные поля
}
```

**Плюсы:**
- Использование существующей модели `Publication` вместо создания отдельной таблицы
- `projectId` как nullable поле - логичное решение для разделения личных и проектных публикаций
- Сохранение всех возможностей публикаций (медиа, source texts, статусы)

**Минусы:**
- ❌ Отсутствует индекс для быстрой выборки личных черновиков: `@@index([createdBy, projectId])`

#### 🔍 Бизнес-логика

**Backend (publications.service.ts:95-118)**

Валидация при создании:
```typescript
if (!data.projectId) {
  if (data.status && 
      data.status !== PublicationStatus.DRAFT && 
      data.status !== PublicationStatus.READY) {
    throw new BadRequestException('Personal drafts can only have DRAFT or READY status');
  }
  if (data.scheduledAt) {
    throw new BadRequestException('Personal drafts cannot be scheduled');
  }
  if (data.channelIds?.length) {
    throw new BadRequestException('Cannot create posts for personal drafts');
  }
}
```

**Оценка:** ✅ **Отлично**
- Четкие ограничения для личных черновиков
- Логичные бизнес-правила (нельзя планировать без проекта)
- Предотвращение создания постов для личных черновиков

**Проблемы:**
- ❌ Нет возможности "повысить" личный черновик до публикации проекта
- ⚠️ Отсутствует явная документация этих ограничений для API пользователей

### 1.2 API эндпоинты

**Специализированный эндпоинт (publications.controller.ts:137-162)**
```typescript
@Get('user/drafts')
public async findUserDrafts(
  @Request() req: UnifiedAuthRequest,
  @Query() query: FindPublicationsQueryDto,
): Promise<PaginatedResponse<any>> {
  // ...
}
```

**Оценка:** ✅ **Хорошо**
- Отдельный эндпоинт для личных черновиков
- Поддержка пагинации и фильтрации
- Автоматическая фильтрация по текущему пользователю

**Проблема:**
- ⚠️ Эндпоинт не документирован в README.md (секция API)

### 1.3 Frontend реализация

**Composable (usePublications.ts:195-227)**
```typescript
async function fetchUserDrafts(
  filters: PublicationsFilter = {}
): Promise<PaginatedPublications> {
  // Корректная реализация с обработкой ошибок
}
```

**Компонент (PublicationForm.vue:150)**
```vue
{ value: null, label: t('publication.personal_draft') }
```

**Оценка:** ✅ **Хорошо**
- Удобный селектор проектов с опцией "Личный черновик"
- Автоматическое применение ограничений в UI
- Реактивное обновление доступных опций

**Проблемы:**
- ⚠️ В списках публикаций нет явного визуального индикатора личных черновиков
- ❌ Отсутствует возможность перемещения черновика в проект через UI

### 1.4 Права доступа

**Проверка доступа (publications.service.ts:526-545)**
```typescript
if (publication.projectId) {
  await this.permissions.checkProjectAccess(publication.projectId, userId);
} else {
  // For personal drafts, only creator can see it
  if (publication.createdBy !== userId) {
    throw new NotFoundException('Publication not found');
  }
}
```

**Оценка:** ✅ **Отлично**
- Строгая проверка владения для личных черновиков
- Использование 404 вместо 403 для предотвращения утечки информации
- Корректная обработка в методах update и delete

---

## 2. Шаблоны промптов (LLM Prompt Templates)

### 2.1 Архитектура и модель данных

**Схема БД (prisma/schema.prisma:288-311)**
```prisma
model LlmPromptTemplate {
  id          String   @id @default(uuid())
  userId      String?  @map("user_id")      // Личные шаблоны
  projectId   String?  @map("project_id")   // Проектные шаблоны
  name        String
  description String?
  prompt      String
  order       Int      @default(0)
  // ...
}
```

**Оценка:** ✅ **Отлично**
- Четкое разделение личных и проектных шаблонов
- Поддержка сортировки через поле `order`
- Правильные индексы для быстрой выборки
- Каскадное удаление при удалении пользователя/проекта

**Плюсы:**
- Отдельная таблица (не смешивается с Publications)
- Гибкая система владения (user OR project)
- Возможность описания и упорядочивания

### 2.2 Backend реализация

**Сервис (llm-prompt-templates.service.ts)**

**Валидация (строки 20-39)**
```typescript
if (createDto.userId && createDto.projectId) {
  throw new BadRequestException('Cannot specify both userId and projectId');
}
if (!createDto.userId && !createDto.projectId) {
  throw new BadRequestException('Must specify either userId or projectId');
}
if (createDto.prompt.length > MAX_PROMPT_LENGTH) {
  throw new BadRequestException(`Prompt is too long. Maximum length is ${MAX_PROMPT_LENGTH} characters`);
}
```

**Оценка:** ✅ **Отлично**
- Взаимоисключающая валидация userId/projectId
- Ограничение длины промпта (5000 символов)
- Автоматический расчет порядка при создании

**Автоматический order (строки 42-49)**
```typescript
let order = createDto.order ?? 0;
if (order === 0) {
  const maxOrder = await this.getMaxOrder(createDto.userId, createDto.projectId);
  order = maxOrder + 1;
}
```

**Оценка:** ✅ **Хорошо**
- Автоматическое добавление в конец списка
- Возможность явного указания порядка

**Проверка прав (verifyOwnership, строки 258-289)**
```typescript
async verifyOwnership(templateId: string, userId: string): Promise<boolean> {
  // Проверка личных шаблонов
  if (template.userId === userId) {
    return true;
  }
  // Проверка проектных шаблонов (членство или владение)
  if (template.projectId) {
    const isMember = template.project?.members && template.project.members.length > 0;
    const isOwner = template.project?.ownerId === userId;
    return isMember || isOwner;
  }
  return false;
}
```

**Оценка:** ✅ **Отлично**
- Корректная проверка для обоих типов шаблонов
- Учет членства в проекте
- Используется в guard'е для защиты эндпоинтов

**Reorder функция (строки 175-247)**
```typescript
async reorder(ids: string[], userId: string) {
  // 1. Проверка существования всех шаблонов
  // 2. Валидация одного scope (все личные ИЛИ все проектные)
  // 3. Проверка прав доступа
  // 4. Транзакционное обновление
}
```

**Оценка:** ✅ **Отлично**
- Транзакционность операции
- Валидация единого scope
- Проверка прав для каждого шаблона

**Проблемы:**
- ⚠️ Отсутствует ограничение на максимальное количество шаблонов на пользователя/проект
- ⚠️ Нет валидации содержимого промпта (например, на опасные инъекции)

### 2.3 Контроллер и безопасность

**Guard (template-ownership.guard.ts)**
```typescript
@Injectable()
export class TemplateOwnershipGuard implements CanActivate {
  async canActivate(context: ExecutionContext): Promise<boolean> {
    const request = context.switchToHttp().getRequest();
    const templateId = request.params.id;
    const userId = request.user.id;
    
    const hasAccess = await this.templatesService.verifyOwnership(templateId, userId);
    if (!hasAccess) {
      throw new ForbiddenException('You do not have access to this template');
    }
    return true;
  }
}
```

**Оценка:** ✅ **Отлично**
- Отдельный guard для проверки владения
- Используется на эндпоинтах update, delete, findOne
- Корректная обработка ошибок доступа

**Контроллер (llm-prompt-templates.controller.ts:36-46)**
```typescript
@Get('user/:userId')
findAllByUser(@Param('userId') userId: string, @Request() req: any) {
  if (req.user.id !== userId) {
    throw new ForbiddenException('You can only access your own templates');
  }
  return this.llmPromptTemplatesService.findAllByUser(userId);
}
```

**Оценка:** ✅ **Хорошо**
- Проверка, что пользователь запрашивает только свои шаблоны
- Предотвращение доступа к чужим личным шаблонам

**Проблема:**
- ⚠️ Эндпоинт `/project/:projectId` не проверяет членство в проекте на уровне контроллера (только комментарий)

### 2.4 Frontend реализация

**Компонент управления (SettingsLlmPromptTemplates.vue)**

**Плюсы:**
- ✅ Drag & drop для изменения порядка
- ✅ Поиск по шаблонам
- ✅ Модальные окна для создания/редактирования
- ✅ Подтверждение удаления
- ✅ Пустое состояние с призывом к действию
- ✅ Отдельная обработка личных и проектных шаблонов

**Проблемы:**
- ⚠️ Счетчик символов отображается только в модальном окне, но не при просмотре списка
- ⚠️ Нет предпросмотра результата применения шаблона

**Интеграция в LLM модал (LlmGeneratorModal.vue:116-153)**
```vue
const templateOptions = computed(() => {
  const options = [{ label: t('llm.noTemplate'), value: null }]
  
  if (userTemplates.value.length > 0) {
    options.push({ label: t('llm.personalTemplates'), value: 'personal-header', disabled: true })
    userTemplates.value.forEach(template => {
      options.push({ label: `  ${template.name}`, value: template.id })
    })
  }
  
  if (projectTemplates.value.length > 0) {
    options.push({ label: t('llm.projectTemplates'), value: 'project-header', disabled: true })
    projectTemplates.value.forEach(template => {
      options.push({ label: `  ${template.name}`, value: template.id })
    })
  }
  
  return options
})
```

**Оценка:** ✅ **Отлично**
- Группировка личных и проектных шаблонов
- Визуальное разделение через заголовки
- Автоматическая загрузка при открытии модала
- Добавление промпта к существующему тексту (не замена)

### 2.5 Тестирование

**Unit тесты (llm-prompt-templates.service.spec.ts)**

Покрытие:
- ✅ Создание шаблонов (личных и проектных)
- ✅ Валидация взаимоисключающих полей
- ✅ Валидация длины промпта
- ✅ Автоматический расчет order
- ✅ Получение шаблонов по пользователю/проекту
- ✅ Обновление и удаление
- ✅ Проверка прав доступа (verifyOwnership)
- ✅ Reorder функциональность

**Оценка:** ✅ **Отлично**
- Полное покрытие основной функциональности
- Тестирование edge cases
- Проверка безопасности

---

## 3. Взаимосвязь функциональностей

### 3.1 Концептуальное разделение

**Личные черновики (Publications):**
- Назначение: Хранение неопубликованного контента
- Содержимое: Полноценные публикации с медиа, текстом, метаданными
- Жизненный цикл: Создание → Редактирование → (Перенос в проект) → Публикация
- Доступ: Только владелец

**Шаблоны промптов (LlmPromptTemplates):**
- Назначение: Переиспользуемые заготовки для AI генерации
- Содержимое: Только текст промпта + метаданные
- Жизненный цикл: Создание → Использование в генерации → Редактирование/Удаление
- Доступ: Владелец (личные) или участники проекта (проектные)

**Оценка:** ✅ **Логично и правильно**
- Четкое разделение ответственности
- Разные модели данных для разных целей
- Нет дублирования функциональности

### 3.2 Терминология

**Проблема:** ⚠️ **Потенциальная путаница**

В русском языке:
- "Черновик" (draft) - незавершенная публикация
- "Заготовка" (template) - переиспользуемый шаблон

В коде используется:
- `Publication` с `status: DRAFT` для черновиков
- `LlmPromptTemplate` для шаблонов промптов

**Рекомендация:**
- В UI четко разделять: "Личные черновики" vs "Шаблоны промптов"
- В документации явно объяснять разницу
- Избегать термина "заготовки" для Publications

---

## 4. Найденные проблемы и рекомендации

### 4.1 Критические проблемы

**Нет критических проблем** ✅

### 4.2 Важные улучшения

#### 1. Отсутствие конвертации личного черновика в публикацию проекта

**Проблема:**
Пользователь создал личный черновик, но не может перенести его в проект без копирования.

**Решение:**
```typescript
// В publications.service.ts
async convertToProjectPublication(
  publicationId: string,
  userId: string,
  targetProjectId: string
): Promise<Publication> {
  const publication = await this.findOne(publicationId, userId);
  
  if (publication.projectId) {
    throw new BadRequestException('Publication already belongs to a project');
  }
  
  if (publication.createdBy !== userId) {
    throw new ForbiddenException('Only creator can convert personal draft');
  }
  
  await this.permissions.checkProjectAccess(targetProjectId, userId);
  
  return this.prisma.publication.update({
    where: { id: publicationId },
    data: { projectId: targetProjectId }
  });
}
```

**Приоритет:** Средний  
**Сложность:** Низкая

#### 2. Отсутствие индекса для личных черновиков

**Проблема:**
Запросы личных черновиков могут быть медленными при большом количестве публикаций.

**Решение:**
```prisma
model Publication {
  // ...
  @@index([createdBy, projectId])  // Добавить этот индекс
  @@index([projectId, status])
  // ...
}
```

**Приоритет:** Средний  
**Сложность:** Низкая

#### 3. Недостаточная документация API

**Проблема:**
Эндпоинт `/publications/user/drafts` не описан в README.md

**Решение:**
Добавить в секцию API документации:

```markdown
### Personal Drafts

**GET** `/api/v1/publications/user/drafts`

Получение личных черновиков текущего пользователя.

Query параметры:
- `status` - фильтр по статусу (DRAFT, READY)
- `limit` - количество записей (по умолчанию 20)
- `offset` - смещение для пагинации
- `sortBy` - поле для сортировки
- `sortOrder` - направление сортировки (asc, desc)
- `search` - поиск по title и content
```

**Приоритет:** Средний  
**Сложность:** Низкая

### 4.3 Желательные улучшения

#### 1. Визуальный индикатор личных черновиков

**Проблема:**
В списках публикаций не всегда очевидно, что это личный черновик.

**Решение:**
Добавить badge или иконку:
```vue
<UBadge v-if="!publication.projectId" color="neutral" variant="soft">
  {{ t('publication.personal') }}
</UBadge>
```

**Приоритет:** Низкий  
**Сложность:** Низкая

#### 2. Ограничение количества шаблонов

**Проблема:**
Нет ограничения на количество шаблонов, что может привести к захламлению.

**Решение:**
```typescript
const MAX_USER_TEMPLATES = 50;
const MAX_PROJECT_TEMPLATES = 100;

async create(createDto: CreateLlmPromptTemplateDto) {
  const count = await this.prisma.llmPromptTemplate.count({
    where: createDto.userId ? { userId: createDto.userId } : { projectId: createDto.projectId }
  });
  
  const limit = createDto.userId ? MAX_USER_TEMPLATES : MAX_PROJECT_TEMPLATES;
  if (count >= limit) {
    throw new BadRequestException(`Maximum ${limit} templates allowed`);
  }
  // ...
}
```

**Приоритет:** Низкий  
**Сложность:** Низкая

#### 3. Валидация содержимого промпта

**Проблема:**
Нет проверки на потенциально опасное содержимое.

**Решение:**
```typescript
private validatePromptContent(prompt: string): void {
  // Базовая проверка на SQL инъекции, XSS и т.д.
  const dangerousPatterns = [
    /<script/i,
    /javascript:/i,
    /on\w+\s*=/i,  // event handlers
  ];
  
  for (const pattern of dangerousPatterns) {
    if (pattern.test(prompt)) {
      throw new BadRequestException('Prompt contains potentially dangerous content');
    }
  }
}
```

**Приоритет:** Низкий  
**Сложность:** Средняя

#### 4. Статистика использования шаблонов

**Проблема:**
Нет информации о том, какие шаблоны используются чаще всего.

**Решение:**
Добавить поле `usageCount` и `lastUsedAt` в модель:
```prisma
model LlmPromptTemplate {
  // ...
  usageCount Int @default(0) @map("usage_count")
  lastUsedAt DateTime? @map("last_used_at")
}
```

**Приоритет:** Низкий  
**Сложность:** Средняя

---

## 5. Соответствие лучшим практикам

### 5.1 Архитектура

✅ **Separation of Concerns** - четкое разделение личных черновиков и шаблонов  
✅ **Single Responsibility** - каждая сущность имеет одну ответственность  
✅ **DRY** - переиспользование модели Publication для личных черновиков  
✅ **SOLID принципы** - соблюдены в сервисах и контроллерах

### 5.2 Безопасность

✅ **Authentication** - все эндпоинты защищены  
✅ **Authorization** - проверка прав на уровне сервисов и guard'ов  
✅ **Input Validation** - валидация DTO через class-validator  
✅ **Error Handling** - корректная обработка ошибок доступа (404 vs 403)  
⚠️ **Content Validation** - отсутствует для промптов

### 5.3 Производительность

✅ **Индексы БД** - присутствуют для основных запросов  
⚠️ **Кэширование** - не реализовано для шаблонов (но может быть избыточно)  
✅ **Пагинация** - реализована для всех списков  
⚠️ **N+1 проблема** - отсутствует в текущей реализации

### 5.4 Тестирование

✅ **Unit тесты** - покрытие критической функциональности  
✅ **Edge cases** - протестированы граничные случаи  
⚠️ **E2E тесты** - отсутствуют для личных черновиков  
⚠️ **Frontend тесты** - отсутствуют

### 5.5 UX/UI

✅ **Интуитивность** - понятный интерфейс  
✅ **Обратная связь** - toast уведомления и индикаторы загрузки  
✅ **Пустые состояния** - реализованы для шаблонов  
⚠️ **Визуальные индикаторы** - недостаточно для личных черновиков  
✅ **Drag & Drop** - реализовано для сортировки шаблонов

---

## 6. Итоговая оценка

### Общая оценка: **8.5/10** ✅

**Сильные стороны:**
1. Продуманная архитектура с четким разделением ответственности
2. Надежная система безопасности и прав доступа
3. Хорошее покрытие тестами критической функциональности
4. Удобный и интуитивный UI
5. Соблюдение лучших практик разработки

**Области для улучшения:**
1. Документация (особенно API для личных черновиков)
2. Возможность конвертации личного черновика в публикацию проекта
3. Визуальные индикаторы в UI
4. Валидация содержимого промптов
5. E2E тестирование

### Рекомендации по приоритетам

**Высокий приоритет:**
1. ✅ Все критические функции реализованы корректно

**Средний приоритет:**
1. Добавить конвертацию личных черновиков в публикации проектов
2. Улучшить документацию API
3. Добавить индекс для личных черновиков

**Низкий приоритет:**
1. Визуальные улучшения UI
2. Ограничения на количество шаблонов
3. Статистика использования шаблонов
4. Валидация содержимого промптов

---

## 7. Заключение

Функциональность личных черновиков и шаблонов промптов реализована **качественно и логично**. Архитектура продумана, код чистый и поддерживаемый, безопасность на высоком уровне.

**Нет критических проблем**, требующих немедленного исправления.

Найденные замечания носят характер **улучшений и оптимизаций**, которые могут быть реализованы по мере необходимости.

Система готова к использованию в продакшене.
